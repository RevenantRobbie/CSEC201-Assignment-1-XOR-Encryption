1: Review the overview and create a bulleted list of requirements for this program
	1) be able to correctly xor a plaintext with a ciphertext
	2) be able to correctly decrypt an xor ciphertext given the correct key

2: Use your answer #1 to create a list of tests
	1) given a simple ciphertext with 100 characters and a simple key, test if the program is able to xor correctly
	2) given said ciphertext, test if the same key is able to decrypt the ciphertext successfully
	

3: Create a bulleted list of 5 requirements for this program that involve edge cases and/or error handling
	1) In the event of recieving an extremely large plaintext, the code does not buffer overflow
	2) In the event of the key not containing a null byte, the code does not read an infinitely large key
	3) In the event of a key that is larger than 32 characters, the code should not read past the 32nd character
	4) In the event of a filename that is larger than 100 characters, the code should not crash or buffer overflow
	5) In the event of filename or key not ending in \n, strcspn does not cause the program to break

4: Use answer #3 to create a list of tests you need to perform to verify the edge cases
	1) create and use a 1000 byte plaintext
	2) create a key that does not end in a null byte 
	3) create a key that is 500 bytes long
	4) encrypt a filename with a size of 500 bytes
	5) encrypt a valid filename that does not end in \n or \0
5: see SuccessfulCompile.png
6:
	Test 1 failed due to being unable to accept any file input
	Test 2 failed for the same reason
	Both tests shared the same fix
7: 
	error on non edgecase test 1) cannot take file input and thus, unable to read from file or write to file.
		solution: newline character from fgets was carrying over, so I had to add a quick getchar() in there to consume the newline
	noticed that the encrpytion program will also encrpyt the newline character at the end of a ciphertext. This isn't nessecarily an error, but it's good to note
	all other standard test cases past correctly, moving onto edge case test cases

8:
	edgecase test 1 does not write to file if input is greater than 256 bytes.
	edgecase test 2 does not output a plaintext if key does not contain a newline or null byte
	edgecase test 3 does not attempt to decrypt plaintext if key is greater than 32 bytes
	edgecase test 4 creates a file with a maoe of 100 As, however no encrypted text is written to the file
	edgecase test 5 fails to create a file and freezes if the file does not end with a null byte or newline



	